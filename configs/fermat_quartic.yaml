target: fermat_quartic
seed: 20260707
n_samples: 240
include_orbits: true
splits:
  train: 0.7
  val: 0.15
invariants:
  power_max: 6
models:
  log_sum_terms: 6
validation:
  geometry_max_points: 48
  finite_difference_step: 1.0e-4
training:
  invariant_potential:
    enabled: true
    basis_names: [p2, p3, p4, e2, e3, centered_l2]
    max_train_points: 36
    max_val_points: 48
    maxiter: 60
    model_path: artifacts/models/fermat_quartic_invariant_potential.json
outputs:
  experiment_dir: data/experiments/fermat_quartic_smoke
